var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/#API","page":"API References","text":"","title":"API"},{"category":"section","location":"api/#Weight-types","page":"API References","text":"","title":"Weight types"},{"category":"function","location":"api/#LsqFit.curve_fit","page":"API References","text":"curve_fit(model, xdata, ydata, p0) -> fit\ncurve_fit(model, xdata, ydata, wt, p0) -> fit\n\nFit data to a non-linear model. p0 is an initial model parameter guess (see Example), and wt is an optional weighting of the observations. The return object is a composite type (LsqFitResult), with some interesting values:\n\nfit.resid : residuals = vector of residuals\nfit.jacobian : estimated Jacobian at solution\n\nadditionally, it is possible to query the degrees of freedom with\n\ndof(fit)\ncoef(fit)\n\nWeights\n\nThe values of wt are inverse variances, not standard deviations. A vector weights the squared residuals ∑ wtᵢ (model−y)ᵢ², so the optimal choice is wtᵢ = 1/σᵢ². A matrix is the inverse covariance Σ⁻¹ of the observations (a diagonal of 1/σᵢ² is the vector case).\n\nThe type of wt sets whether the residual scale σ² is known or estimated:\n\nwt scale σ² vcov(fit)\nomitted estimated σ̂² (JᵀJ)⁻¹\nPrecisionWeights(1 ./ σ.^2) known (JᵀWJ)⁻¹\nPrecisionMatrix(inv(Σ)) known (JᵀWJ)⁻¹\nAnalyticWeights(1 ./ σ.^2) estimated σ̂² (JᵀWJ)⁻¹\nFrequencyWeights(counts) estimated, nobs=∑w σ̂² (JᵀWJ)⁻¹\n\nAnalyticWeights are relative inverse-variance weights and do not depend on the overall scale of the weights, matching MATLAB nlinfit, Origin and LabPlot. PrecisionWeights are the exact inverse variances and PrecisionMatrix the exact inverse covariance, with the scale known; both use the normal critical value for confidence intervals. vcov(fit) is the parameter covariance and stderror(fit) its square-rooted diagonal. The \"Weights\" page of the manual has the derivation and a coverage check.\n\nwarning: Deprecated\nPassing a bare inverse-variance Vector or inverse-covariance Matrix is deprecated. Use PrecisionWeights(1 ./ σ.^2) / PrecisionMatrix(inv(Σ)) for the same known-variance covariance (with a calibrated normal interval), or AnalyticWeights to estimate the scale.\n\nExample\n\n# a two-parameter exponential model\n# x: array of independent variables\n# p: array of model parameters\nmodel(x, p) = p[1]*exp.(-x.*p[2])\n\n# some example data\n# xdata: independent variables\n# ydata: dependent variable\nxdata = range(0, stop=10, length=20)\nydata = model(xdata, [1.0 2.0]) + 0.01*randn(length(xdata))\np0 = [0.5, 0.5]\n\nfit = curve_fit(model, xdata, ydata, p0)\n\n\n\n\n\n","title":"LsqFit.curve_fit"},{"category":"type","location":"api/#LsqFit.PrecisionWeights","page":"API References","text":"PrecisionWeights(vs)\n\nWeights whose values are the exact, known inverse variances 1 ./ σ.^2 of the observations. The residual scale is treated as known rather than estimated, so vcov is (JᵀWJ)⁻¹ with no mean-squared-error factor and confidence intervals use the normal (asymptotic) critical value.\n\nThis is the typed equivalent of passing a bare inverse-variance vector. Use AnalyticWeights instead when the weights are only relative precisions and the scale should be estimated.\n\n\n\n\n\n","title":"LsqFit.PrecisionWeights"},{"category":"type","location":"api/#LsqFit.PrecisionMatrix","page":"API References","text":"PrecisionMatrix(m)\n\nThe exact, known precision matrix (inverse covariance) inv(Σ) of correlated observations. This is the matrix counterpart of PrecisionWeights: the residual scale is treated as known, so vcov is (JᵀWJ)⁻¹ with no mean-squared-error factor and confidence intervals use the normal critical value.\n\nnote: Note\nThe argument is the precision matrix inv(Σ), not the covariance Σ.\n\n\n\n\n\n","title":"LsqFit.PrecisionMatrix"},{"category":"section","location":"getting_started/#Getting-Started","page":"Getting Started","text":"First, import the package.\n\njulia> using LsqFit\n\nDefine a two-parameter exponential decay model, where t is a one-element independent variable, p_1 and p_2 are parameters.\n\nThe model function is:\n\nm(t boldsymbolp) = p_1 exp(-p_2 t)\n\n# t: array of independent variable\n# p: array of model parameters\nmodel(t, p) = p[1] * exp.(-p[2] * t)\n\nFor illustration purpose, we generate some fake data.\n\n# tdata: data of independent variable\ntdata = range(0, stop=10, length=20)\n# ydata: data of dependent variable\nydata = model(tdata, [1.0 2.0]) + 0.01*randn(length(tdata))\n\nBefore fitting the data, we also need a initial value of parameters for curve_fit().\n\np0 = [0.5, 0.5]\n\nRun curve_fit() to fit the data and get the estimated parameters.\n\njulia> fit = curve_fit(model, tdata, ydata, p0)\njulia> param = fit.param\n2-element Vector{Float64}:\n 1.01105\n 2.0735\n\nLsqFit.jl also provides functions to examine the goodness of fit. vcov(fit) computes the estimated covariance matrix.\n\njulia> cov = vcov(fit)\n2×2 Matrix{Float64}:\n 0.000116545  0.000174633\n 0.000174633  0.00258261\n\nstderror(fit) returns the standard error of each parameter.\n\njulia> se = stderror(fit)\n2-element Vector{Float64}:\n 0.0107956\n 0.0508193\n\nTo get the confidence interval at 10% significance level, run confint(fit; level=0.9), which essentially computes the estimate parameter value ± (standard error * critical value from t-distribution).\n\njulia> confidence_interval = confint(fit; level=0.9)\n2-element Vector{Tuple{Float64,Float64}}:\n (0.992333, 1.02977)\n (1.98537, 2.16162)\n\nFor more details of LsqFit.jl, check Tutorial and API References section.","title":"Getting Started"},{"category":"section","location":"weights/#Weights","page":"Weights","text":"This page describes how weighted fits affect the reported parameter uncertainties, how the weight values and weight types are interpreted, and a Monte-Carlo check of the resulting confidence intervals.\n\nTwo points cover most of the confusion around weighted fits:\n\nThe weight values are inverse variances (wᵢ = 1/σᵢ²), not standard deviations.\nThe weight type decides whether the noise scale σ² is treated as known or estimated. vcov(fit) returns a parameter covariance matrix and stderror(fit) the square root of its diagonal.","title":"Weights"},{"category":"section","location":"weights/#Standard-deviation,-variance-and-covariance","page":"Weights","text":"Quantity Symbol Units Role\nObservation standard deviation σᵢ like yᵢ not passed directly\nObservation variance σᵢ² yᵢ² a vector weight is 1/σᵢ²\nObservation covariance Σ yᵢ yⱼ a matrix weight is Σ⁻¹\nParameter covariance Cov(γ̂) pᵢ pⱼ vcov(fit)\nParameter standard error √Cov(γ̂)ₖₖ like pₖ stderror(fit)\n\nThe data uncertainties go in as inverse variances and the parameter uncertainties come out as a covariance matrix whose square-rooted diagonal gives the standard errors.\n\nIf observation i has standard deviation σᵢ, its weight is 1/σᵢ², not 1/σᵢ. The weight multiplies the squared residual ∑ wᵢ (model−y)ᵢ², and the minimum-variance weighting of a squared residual is the inverse of its variance.","title":"Standard deviation, variance and covariance"},{"category":"section","location":"weights/#Weighted-least-squares","page":"Weights","text":"Near the optimum the weighted problem linearises to\n\nhatboldsymbolh = hatboldsymbolγ - boldsymbolγ^*\n    approx -JWJ^-1 J W boldsymbolr\n\nwith J the Jacobian and W the weight matrix. For errors ε ∼ N(0, Σ) and the optimal weights W = Σ⁻¹,\n\nmathbfCov(boldsymbolγ^*)\n    = JWJ^-1 JWSigmaWJJWJ^-1\n    = JWJ^-1\n\nThis is the known-variance case: the exact Σ⁻¹ was supplied, so there is no scale left to estimate.\n\nIf the variances are known only up to a common unknown factor, ε ∼ N(0, σ² W⁻¹), then\n\nmathbfCov(boldsymbolγ^*) = σ^2 JWJ^-1\n\nwith σ² estimated from the residuals as the mean squared error RSS/(n-p) (LsqFit.mse). The same σ² multiplies the whole matrix, so this estimator does not change if all weights are multiplied by a constant. The unweighted fit is the case W = I of this second formula.\n\nBoth formulas are correct; they answer different questions.","title":"Weighted least squares"},{"category":"section","location":"weights/#Weight-types","page":"Weights","text":"LsqFit selects the formula from the type of wt. The values are the same inverse variances in every case.\n\nwt argument scale σ² vcov(fit) nobs\nomitted estimated σ̂² (JᵀJ)⁻¹ n\nPrecisionWeights(1 ./ σ.^2) known (JᵀWJ)⁻¹ n\nPrecisionMatrix(inv(Σ)) known (JᵀWJ)⁻¹ n\nAnalyticWeights(1 ./ σ.^2) estimated σ̂² (JᵀWJ)⁻¹ n\nFrequencyWeights(counts) estimated σ̂² (JᵀWJ)⁻¹ ∑w\n\nwarning: Deprecated: bare vector and matrix weights\nPassing a bare 1 ./ σ.^2 vector or inv(Σ) matrix is deprecated and emits a warning. Wrap them in PrecisionWeights / PrecisionMatrix for the same known-variance covariance (with the calibrated normal interval), or in AnalyticWeights to estimate the scale. The bare forms previously kept a Student-t interval purely for backwards compatibility.\n\nAnalyticWeights, FrequencyWeights are re-exported from StatsBase and keep their StatsBase meaning; PrecisionWeights and PrecisionMatrix are defined by LsqFit.\n\nPrecisionWeights are the exact, known inverse variances (independent errors) and PrecisionMatrix the exact, known precision matrix inv(Σ) (correlated errors). The scale is known, so vcov has no MSE factor and the confidence interval uses the normal critical value. They are the typed forms of a bare vector and a bare matrix.\nAnalyticWeights are reliability or inverse-variance weights. They give a relative importance, so the common scale is estimated and the result does not depend on the overall magnitude of the weights. This is the convention used by MATLAB nlinfit, Origin and LabPlot.\nFrequencyWeights are integer counts: wᵢ means observation i was seen wᵢ times, so nobs = ∑w.\nA bare vector or matrix (deprecated) is taken as the exact inverse (co)variance, like PrecisionWeights/PrecisionMatrix, but keeps the Student-t interval for backwards compatibility. Prefer the typed forms.\n\nStatsBase has no weight type for the known-variance case (all of its corrected weights estimate the scale), which is why PrecisionWeights/PrecisionMatrix are provided here. ProbabilityWeights would require a survey/sandwich variance, and a generic Weights has no associated bias correction in StatsBase. Passing either throws an error rather than returning a covariance that does not match the type.","title":"Weight types"},{"category":"section","location":"weights/#Example","page":"Weights","text":"The dataset below is the one from issue #255. The model is f(x) = A·exp(B·x) with a 5 % relative measurement error.\n\nusing LsqFit\n\nmodel(x, p) = p[1] .* exp.(p[2] .* x)\n\nx = Float64[1, 2, 4, 5, 8]\ny = Float64[3, 4, 6, 11, 20]\nσ = 0.05 .* y          # standard deviations\nwt = 1 ./ σ.^2         # inverse variances\n\nnothing # hide\n\nTaking the weights as exact inverse variances (known scale):\n\nfit_known = curve_fit(model, x, y, PrecisionWeights(wt), [2.0, 0.3])\ncoef(fit_known), stderror(fit_known)\n\nTaking them as relative precisions and estimating the scale:\n\nfit_rel = curve_fit(model, x, y, AnalyticWeights(wt), [2.0, 0.3])\ncoef(fit_rel), stderror(fit_rel)\n\nThe point estimates agree (A ≈ 2.263, B ≈ 0.275); only the standard errors differ. The known-scale (PrecisionWeights) errors are about ±0.097 / ±0.009, the AnalyticWeights errors about ±0.258 / ±0.024. The latter match the values reported by Origin, LabPlot and mycurvefit for this data.\n\nAnalyticWeights do not depend on the overall scale of the weights:\n\nse1 = stderror(curve_fit(model, x, y, AnalyticWeights(wt), [2.0, 0.3]))\nse2 = stderror(curve_fit(model, x, y, AnalyticWeights(10 .* wt), [2.0, 0.3]))\nse1 ≈ se2\n\nPrecisionWeights do depend on the scale, and AnalyticWeights(ones(n)) reproduces the unweighted fit while PrecisionWeights(ones(n)) does not.\n\nFor repeated measurements use FrequencyWeights; nobs is then the total count:\n\nfit_freq = curve_fit(model, x, y, FrequencyWeights([3, 1, 1, 2, 1]), [2.0, 0.3])\nnobs(fit_freq), dof(fit_freq)","title":"Example"},{"category":"section","location":"weights/#Monte-Carlo-coverage","page":"Weights","text":"A 95 % confidence interval should contain the true parameter about 95 % of the time. The following simulates from a known model with known heteroscedastic noise and counts how often confint covers the truth.\n\nusing Random\n\ntruth = [2.0, 0.25]\nxg = collect(range(0.5, 6; length = 15))\nrng = MersenneTwister(20240617)\nN = 4000\n\ncovers(lohi, t) = [lo <= t <= hi for ((lo, hi), t) in zip(lohi, (t[1], t[2]))]\n\nhitV = zeros(2); hitA = zeros(2); hitU = zeros(2)\nfor _ in 1:N\n    sd = 0.10 .* model(xg, truth)\n    yg = model(xg, truth) .+ sd .* randn(rng, length(xg))\n    w  = 1 ./ sd.^2\n    fV = curve_fit(model, xg, yg, PrecisionWeights(w), copy(truth))  # known variance, normal CI\n    fA = curve_fit(model, xg, yg, AnalyticWeights(w), copy(truth))  # estimated scale\n    fU = curve_fit(model, xg, yg, copy(truth))                      # unweighted\n    global hitV += covers(confint(fV; level = 0.95), truth)\n    global hitA += covers(confint(fA; level = 0.95), truth)\n    global hitU += covers(confint(fU; level = 0.95), truth)\nend\nnothing # hide\n\nusing SummaryTables\n\npct(h) = string(round(100 * h / N; digits = 1), \" %\")\ncovrow(name, h) = [Cell(name) Cell(pct(h[1])) Cell(pct(h[2]))]\nbody = vcat(\n    permutedims(Cell.([\"weighting\", \"A coverage\", \"B coverage\"]; bold = true)),\n    covrow(\"PrecisionWeights (known σ², normal)\", hitV),\n    covrow(\"AnalyticWeights (estimated scale)\", hitA),\n    covrow(\"unweighted (ignores heteroscedasticity)\", hitU),\n)\nTable(body; header = 1)\n\nPrecisionWeights and AnalyticWeights are both close to the nominal 95 %, each with the reference distribution that matches its assumption. PrecisionWeights treats the variance as known, so (γ̂ₖ − γₖ)/seₖ is asymptotically normal and confint uses z = 1.96. AnalyticWeights estimate the scale, so the same ratio follows Student-t with dof = n − p. Ignoring the known heteroscedasticity is the case that is actually wrong.\n\nmargin_error and confint use Student-t for the unweighted case. Typed weights use Student-t when the scale is estimated (AnalyticWeights, FrequencyWeights) and the normal quantile when it is known (PrecisionWeights, PrecisionMatrix). A deprecated bare vector or matrix has the same covariance as PrecisionWeights / PrecisionMatrix but keeps the Student-t reference for backwards compatibility.","title":"Monte-Carlo coverage"},{"category":"section","location":"weights/#Choosing-weights","page":"Weights","text":"Known σᵢ, trusted as absolute: PrecisionWeights(1 ./ σ.^2), or PrecisionMatrix(inv(Σ)) for correlated errors. (Passing a bare 1 ./ σ.^2 vector or inv(Σ) matrix is deprecated.)\nOnly relative precisions known, or results that match MATLAB/Origin/LabPlot: AnalyticWeights(1 ./ σ.^2).\nRepeated or aggregated counts: FrequencyWeights(counts).\nNothing better: leave wt out.","title":"Choosing weights"},{"category":"section","location":"#LsqFit.jl","page":"Home","text":"Basic least-squares fitting in pure Julia under an MIT license.\n\nThe basic functionality was originaly in Optim.jl, before being separated into this library.  At this time, LsqFit only utilizes the Levenberg-Marquardt algorithm for non-linear fitting.\n\nLsqFit.jl is part of the JuliaNLSolvers family.\n\nSource Package Evaluator Build Status\n(Image: Source) (Image: LsqFit) (Image: LsqFit) (Image: LsqFit) (Image: LsqFit) (Image: Build Status)","title":"LsqFit.jl"},{"category":"section","location":"#Install","page":"Home","text":"To install the package, run\n\nPkg.add(\"LsqFit\")\n\nIf you want the latest features, also run\n\nPkg.checkout(\"LsqFit\")\n\nTo use the package in your code\n\njulia> using LsqFit","title":"Install"},{"category":"section","location":"tutorial/#Tutorial","page":"Tutorial","text":"","title":"Tutorial"},{"category":"section","location":"tutorial/#Introduction-to-Nonlinear-Regression","page":"Tutorial","text":"Assume that, for the i-th observation, the relationship between independent variable mathbfx_i = beginbmatrix x_1i x_2i  x_pi endbmatrix and dependent variable Y_i follows:\n\nY_i = m(mathbfx_i boldsymbolγ) + ε_i\n\nwhere m is a non-linear model function depends on the independent variable mathbfx_i and the parameter vector boldsymbolγ. In order to find the parameter boldsymbolγ that \"best\" fit our data, we choose the parameter boldsymbolγ which minimizes the sum of squared residuals from our data, i.e. solves the problem:\n\nmin_boldsymbolγ s(boldsymbolγ) = sum_i=1^n leftm(mathbfx_i boldsymbolγ) - y_iright^2\n\nGiven that the function m is non-linear, there's no analytical solution for the best boldsymbolγ. We have to use computational tools, which is LsqFit.jl in this tutorial, to find the least squares solution.\n\nOne example of non-linear model is the exponential model, which takes a one-element predictor variable t. The model function is:\n\nm(t boldsymbolγ) = γ_1 exp(γ_2 t)\n\nand the model becomes:\n\nY_i = γ_1 exp(γ_2 t_i) + ε_i\n\nTo fit data using LsqFit.jl, pass the defined model function (m), data (tdata and ydata) and the initial parameter value (p0) to curve_fit(). For now, LsqFit.jl only supports the Levenberg Marquardt algorithm.\n\n# t: array of independent variables\n# p: array of model parameters\nm(t, p) = p[1] * exp.(p[2] * t)\np0 = [0.5, 0.5]\nfit = curve_fit(m, tdata, ydata, p0)\n\nIt will return a composite type LsqFitResult, with some interesting values:\n\ndof(fit): degrees of freedom\ncoef(fit): best fit parameters\nfit.resid: vector of residuals\nfit.jacobian: estimated Jacobian at the solution","title":"Introduction to Nonlinear Regression"},{"category":"section","location":"tutorial/#Jacobian-Calculation","page":"Tutorial","text":"The Jacobian J_mathbff(mathbfx) of a vector function mathbff(mathbfx) mathbbR^m to mathbbR^n is defined as the matrix with elements:\n\nJ_mathbff(mathbfx)_ij = fracf_i(mathbfx)x_j\n\nThe matrix is therefore:\n\nJ_f(mathbfx) = beginbmatrix\n    fracf_1x_1  fracf_1x_2    fracf_1x_m \n    fracf_2x_1  fracf_2x_2    fracf_2x_m \n                                                           \n    fracf_nx_1  fracf_nx_2    fracf_nx_m\nendbmatrix\n\nThe Jacobian of the exponential model function with respect to boldsymbolγ is:\n\nJ_m(t boldsymbolγ)\n= beginbmatrix fracmγ_1  fracmγ_2 endbmatrix\n= beginbmatrix exp(γ_2 t)  t γ_1 exp(γ_2 t) endbmatrix\n\nBy default, the finite differences is used (see NLSolversBase.jl for more information), is used to approximate the Jacobian for the data fitting algorithm and covariance computation. Alternatively, a function which calculates the Jacobian can be supplied to curve_fit() for faster and/or more accurate results.\n\nfunction j_m(t,p)\n    J = zeros(length(t),length(p))\n    J[:,1] = exp.(p[2] .* t)       #df/dp[1]\n    J[:,2] = t .* p[1] .* J[:,1]   #df/dp[2]\n    J\nend\n\nfit = curve_fit(m, j_m, tdata, ydata, p0)","title":"Jacobian Calculation"},{"category":"section","location":"tutorial/#Linear-Approximation","page":"Tutorial","text":"The non-linear function m can be approximated as a linear function by Taylor expansion:\n\nm(mathbfx_i boldsymbolγ+boldsymbolh)  m(mathbfx_i boldsymbolγ) +   m(mathbfx_i boldsymbolγ) boldsymbolh\n\nwhere boldsymbolγ is a fixed vector, boldsymbolh is a very small-valued vector and  m(mathbfx_i boldsymbolγ) is the gradient at mathbfx_i.\n\nConsider the residual vector function boldsymbolr(boldsymbolγ) = beginbmatrix r_1(boldsymbolγ)  r_2(boldsymbolγ)    r_n(boldsymbolγ) endbmatrix with entries:\n\nr_i(boldsymbolγ) = m(mathbfx_i boldsymbolγ) - Y_i\n\nEach entry's linear approximation can hence be written as:\n\nbeginalign*\nr_i(boldsymbolγ+boldsymbolh) = m(mathbfx_i boldsymbolγ+boldsymbolh) - Y_i\n m(mathbfx_i boldsymbolγ) +  m(mathbfx_i boldsymbolγ) boldsymbolh - Y_i\n= r_i(boldsymbolγ) +  m(mathbfx_i boldsymbolγ) boldsymbolh\nendalign*\n\nSince the ith row of J(boldsymbolγ) equals the transpose of the gradient of m(mathbfx_i boldsymbolγ), the vector function boldsymbolr(boldsymbolγ + boldsymbolh) can be approximated as:\n\nboldsymbolr(boldsymbolγ+boldsymbolh)  boldsymbolr(boldsymbolγ) + J(boldsymbolγ)boldsymbolh\n\nwhich is a linear function on boldsymbolh since boldsymbolγ is a fixed vector.","title":"Linear Approximation"},{"category":"section","location":"tutorial/#Goodness-of-Fit","page":"Tutorial","text":"The linear approximation of the non-linear least squares problem leads to the approximation of the covariance matrix of each parameter, from which we can perform regression analysis.\n\nConsider a least squares solution boldsymbolγ^*, which is a local minimizer of the non-linear problem:\n\nboldsymbolγ^* = argmin_boldsymbolγ sum_i=1^n leftm(mathbfx_i boldsymbolγ) - y_iright^2\n\nSet boldsymbolγ^* as the fixed point in linear approximation, boldsymbolr(boldsymbolγ^*) = boldsymbolr and J(boldsymbolγ^*) = J. A parameter vector near boldsymbolγ^* can be expressed as boldsymbolγ = boldsymbolγ^* + boldsymbolh. The local approximation for the least squares problem is:\n\nmin_boldsymbolγ s(boldsymbolγ) = s(boldsymbolγ^* + boldsymbolh)  left(Jboldsymbolh + boldsymbolrright) left(Jboldsymbolh + boldsymbolrright)\n\nwhich is essentially the linear least squares problem:\n\nmin_boldsymbolβ left(Xboldsymbolβ - boldsymbolYright) left(Xboldsymbolβ - boldsymbolYright)\n\nwhere X=J, boldsymbolβ = boldsymbolh and boldsymbolY = -boldsymbolr(boldsymbolγ). Solve the equation where the partial derivatives equal to 0, the analytical solution is:\n\nhatboldsymbolh = hatboldsymbolγ - boldsymbolγ^*  -JJ^-1 J boldsymbolr\n\nThe covariance matrix for the analytical solution is:\n\nmathbfCov(hatboldsymbolγ) = mathbfCov(boldsymbolh) = JJ^-1J mathbfE(boldsymbolrboldsymbolr) J JJ^-1\n\nNote that boldsymbolr is the residual vector at the best fit point boldsymbolγ^*, with entries r_i = Y_i - m(mathbfx_i boldsymbolγ^*) = ε_i. hatboldsymbolγ is very close to boldsymbolγ^* and therefore can be replaced by boldsymbolγ^*.\n\nmathbfCov(boldsymbolγ^*)  mathbfCov(hatboldsymbolγ)\n\nAssume the errors in each sample are independent, normal distributed with zero mean and same variance, i.e. ε sim N(0 σ^2I), the covariance matrix from the linear approximation is therefore:\n\nmathbfCov(boldsymbolγ^*) = JJ^-1JmathbfCov(ε)JJJ^-1 = σ^2JJ^-1\n\nwhere σ^2 could be estimated as residual sum of squares divided by degrees of freedom:\n\nhatσ^2=fracs(boldsymbolγ^*)n-p\n\nIn LsqFit.jl, the covariance matrix calculation uses QR decomposition to be more computationally stable, which has the form:\n\nmathbfCov(boldsymbolγ^*) = hatσ^2 mathrmR^-1(mathrmR^-1)\n\nvcov() computes the covariance matrix of fit:\n\njulia> cov = vcov(fit)\n2×2 Matrix{Float64}:\n 0.000116545  0.000174633\n 0.000174633  0.00258261\n\nThe standard error is then the square root of each diagonal elements of the covariance matrix. stderror() returns the standard error of each parameter:\n\njulia> se = stderror(fit)\n2-element Vector{Float64}:\n 0.0114802\n 0.0520416\n\nmargin_error() computes the product of standard error and the critical value of each parameter at a certain significance level (default is 5%) from t-distribution. The margin of error at 10% significance level can be computed by:\n\njulia> margin_of_error = margin_error(fit, 0.1)\n2-element Vector{Float64}:\n 0.0199073\n 0.0902435\n\nconfint() returns the confidence interval of each parameter at certain significance level, which is essentially the estimate value ± margin of error. To get the confidence interval at 10% significance level, run:\n\njulia> confidence_intervals = confint(fit; level=0.9)\n2-element Vector{Tuple{Float64,Float64}}:\n (0.976316, 1.01613)\n (1.91047, 2.09096)","title":"Goodness of Fit"},{"category":"section","location":"tutorial/#Weighted-Least-Squares","page":"Tutorial","text":"curve_fit() also accepts weight parameter (wt) to perform Weighted Least Squares and General Least Squares, where the parameter boldsymbolγ^* minimizes the weighted residual sum of squares.\n\nWeight parameter (wt) is an array or a matrix of weights for each sample. To perform Weighted Least Squares, pass the weight array [w_1, w_2, ..., w_n] or the weight matrix W:\n\nmathbfW = beginbmatrix\n    w_1  0      0 \n    0    w_2    0 \n               \n    0    0      w_n\n    endbmatrix\n\nThe weighted least squares problem becomes:\n\nmin_boldsymbolγ s(boldsymbolγ) = sum_i=1^n w_i leftm(mathbfx_i boldsymbolγ) - Y_iright^2\n\nin matrix form:\n\nmin_boldsymbolγ s(boldsymbolγ) = boldsymbolr(boldsymbolγ) W boldsymbolr(boldsymbolγ)\n\nwhere r(boldsymbolγ) = beginbmatrix r_1(boldsymbolγ)  r_2(boldsymbolγ)   r_n(boldsymbolγ) endbmatrix is a residual vector function with entries:\n\nr_i(boldsymbolγ) = m(mathbfx_i boldsymbolγ) - Y_i\n\nThe algorithm in LsqFit.jl will then provide a least squares solution boldsymbolγ^*.\n\nnote: Note\nIn LsqFit.jl, the residual function passed to levenberg_marquardt() is in different format, if the weight is a vector:r(p) = sqrt.(wt) .* ( model(xpts, p) - ydata )\nlmfit(r, g, p0, wt; kwargs...)r_i(boldsymbolγ) = sqrtw_i cdot m(mathbfx_i boldsymbolγ) - Y_iCholesky decomposition, which is effectively a sqrt of a matrix, will be performed if the weight is a matrix:u = chol(wt)\nr(p) = u * ( model(xpts, p) - ydata )\nlmfit(r, p0, wt; kwargs...)r_i(boldsymbolγ) = sqrtw_i cdot leftm(mathbfx_i boldsymbolγ) - Y_irightThe solution will be the same as the least squares problem mentioned in the tutorial.\n\nSet boldsymbolr(boldsymbolγ^*) = boldsymbolr and J(boldsymbolγ^*) = J, the linear approximation of the weighted least squares problem is then:\n\nmin_boldsymbolγ s(boldsymbolγ) = s(boldsymbolγ^* + boldsymbolh)\n Jboldsymbolh+boldsymbolr W Jboldsymbolh+boldsymbolr\n\nThe analytical solution to the linear approximation is:\n\nhatboldsymbolh = hatboldsymbolγ - boldsymbolγ^*  -JWJ^-1 J W boldsymbolr\n\nAssume the errors in each sample are independent, normal distributed with zero mean and different variances (heteroskedastic error), i.e. ε sim N(0 Sigma), where:\n\nSigma = beginbmatrix\n         σ_1^2   0         0\n         0       σ_2^2     0\n                            \n         0       0         σ_n^2\n         endbmatrix\n\nWe know the error variance and we set the weight as the inverse of the variance (the optimal weight), i.e. W = Sigma^-1:\n\nmathbfW =  beginbmatrix\n              w_1  0      0 \n              0    w_2    0 \n                         \n              0   0       w_n\n              endbmatrix\n           =  beginbmatrix\n               frac1σ_1^2  0                  0 \n               0                frac1σ_2^2    0 \n                                                  \n               0                0                  frac1σ_n^2\n               endbmatrix\n\nThe covariance matrix is now:\n\nmathbfCov(boldsymbolγ^*)  JWJ^-1JW Sigma WJJWJ^-1 = JWJ^-1\n\nThis is the case in which the error variances are known exactly. Wrap them in PrecisionWeights(1 ./ var(ε)), or in PrecisionMatrix(inv(cov_ε)) for correlated errors:\n\nwt = PrecisionMatrix(inv(cov_ε))\nfit = curve_fit(m, tdata, ydata, wt, p0)\ncov = vcov(fit)\n\nwarning: Deprecated\nPassing a bare 1 ./ var(ε) vector or inv(cov_ε) matrix still works but is deprecated; use PrecisionWeights / PrecisionMatrix instead.\n\nIf the variances are known only up to a common factor, ε sim N(0 σ^2 W^-1) with unknown σ^2, the covariance is\n\nmathbfCov(boldsymbolγ^*) = σ^2JWJ^-1\n\nwith σ^2 estimated from the residuals. This is what MATLAB nlinfit, Origin and LabPlot do. Wrap the same inverse-variance numbers in AnalyticWeights to get it:\n\nfit = curve_fit(m, tdata, ydata, AnalyticWeights(1 ./ var(ε)), p0)\n\nAnalyticWeights do not depend on the overall scale of the weights, so uniform weights reproduce the unweighted fit.\n\nPrecisionWeights and AnalyticWeights are not the same: PrecisionWeights take the weights as exact inverse variances (no scale estimated), AnalyticWeights take them as relative precisions and estimate the scale, and they give different standard errors. PrecisionWeights(ones(n)) is not equivalent to an unweighted fit, but AnalyticWeights(ones(n)) is. The Weights page covers this in detail.\n\nnote: Note\nIf the weight matrix is not a diagonal matrix, General Least Squares will be performed.","title":"Weighted Least Squares"},{"category":"section","location":"tutorial/#General-Least-Squares","page":"Tutorial","text":"Assume the errors in each sample are correlated, normal distributed with zero mean and different variances (heteroskedastic and autocorrelated error), i.e. ε sim N(0 Sigma).\n\nSet the weight matrix as the inverse of the error covariance matrix (the optimal weight), i.e. W = Sigma^-1, we will get the parameter covariance matrix:\n\nmathbfCov(boldsymbolγ^*)  JWJ^-1JW Sigma WJJWJ^-1 = JWJ^-1\n\nPass the matrix inv(covar(ε)), wrapped in PrecisionMatrix, as the weight parameter (wt) to the function curve_fit():\n\nwt = PrecisionMatrix(inv(covar(ε)))\nfit = curve_fit(m, tdata, ydata, wt, p0)\ncov = vcov(fit)","title":"General Least Squares"},{"category":"section","location":"tutorial/#Estimate-the-Optimal-Weight","page":"Tutorial","text":"In most cases, the variances of errors are unknown. To perform Weighted Least Square, we need estimate the variances of errors first, which is the squared residual of i-th sample:\n\nwidehatmathbfVar(ε_i) = widehatmathbfE(ε_i ε_i) = r_i(boldsymbolγ^*)\n\nUnweighted fitting (OLS) will return the residuals we need, since the estimator of OLS is unbiased. Then pass the reciprocal of the residuals as the estimated optimal weight to perform Weighted Least Squares:\n\nfit_OLS = curve_fit(m, tdata, ydata, p0)\nwt = AnalyticWeights(1 ./ fit_OLS.resid)\nfit_WLS = curve_fit(m, tdata, ydata, wt, p0)\ncov = vcov(fit_WLS)","title":"Estimate the Optimal Weight"},{"category":"section","location":"tutorial/#References","page":"Tutorial","text":"Hansen, P. C., Pereyra, V. and Scherer, G. (2013) Least squares data fitting with applications. Baltimore, Md: Johns Hopkins University Press, p. 147-155.\n\nKutner, M. H. et al. (2005) Applied Linear statistical models.\n\nWeisberg, S. (2014) Applied linear regression. Fourth edition. Hoboken, NJ: Wiley (Wiley series in probability and statistics).","title":"References"}]
}
